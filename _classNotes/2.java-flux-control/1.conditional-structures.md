# Conditional Structures

## if-else

```java
if (condition) {
  // code
} else if (condition) {
  // code
} else {
  // code
}
```

## Ternary Operator

```java
condition1 ? trueValue : condition2 ? trueValue : falseValue;
```
